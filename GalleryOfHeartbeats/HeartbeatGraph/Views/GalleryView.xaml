<UserControl x:Class="GalleryOfHeartbeats.Views.GalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:GalleryOfHeartbeats.Views"
             mc:Ignorable="d" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
             d:DesignHeight="800" d:DesignWidth="600">
    <Grid Background="{StaticResource PrimaryHueLightBrush}">
        <Grid>
            <ListBox
                Name="Gallery"
                ItemsSource="{Binding HeartbeatOptions}"
                HorizontalContentAlignment="Stretch">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                        <i:InvokeCommandAction
                            Command="{Binding Command}"
                            CommandParameter="{Binding ElementName=Gallery, Path=SelectedItem}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition></RowDefinition>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Width="Auto" Height="Auto" MinHeight="70" Panel.ZIndex="10">
                                <TextBlock Text="{Binding Name}" Margin="0 0 0 0" Style="{StaticResource MaterialDesignHeadline4TextBlock}" FontSize="20" Foreground="WhiteSmoke"/>
                                <TextBlock Text="{Binding TimeOfRecording}" Margin="0 0 0 10" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" FontSize="15" Foreground="WhiteSmoke"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Panel.ZIndex="-10">
                                <ProgressBar Orientation="Horizontal" Height="Auto" MinHeight="70" Margin="0" Value="25"/>
                            </StackPanel>



                        </Grid>

                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
